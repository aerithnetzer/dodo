# Dodo

## Development Environment

Use `docker-compose up` to create the `dodo` database and api services.

There is current one (1) API service---the dodo-activity-api. It sends
and receives data adhering to the ActivityPub protocol for ingestion into
local library catalogs.
